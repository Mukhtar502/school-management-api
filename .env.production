# ============================================
# PRODUCTION ENVIRONMENT - RENDER DEPLOYMENT
# ============================================
# Copy this file and set matching values in Render Dashboard

# Service & Environment
SERVICE_NAME=school-management-api
ENV=production
NODE_ENV=production

# Server Port (Render assigns this automatically, keep as is)
USER_PORT=10000

# ============================================
# MONGODB ATLAS CONFIGURATION
# ============================================
# Get this from MongoDB Atlas (see instructions below)
# Format: mongodb+srv://username:password@cluster.mongodb.net/school_management_system
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@your-cluster.mongodb.net/school_management_system

# ============================================
# REDIS CONFIGURATION
# ============================================
# Render auto-provides these from the redis service in render.yaml
# No need to set manually - Render injects automatically
CACHE_REDIS=auto-injected-by-render
CORTEX_REDIS=auto-injected-by-render
OYSTER_REDIS=auto-injected-by-render

# Prefixes
CORTEX_PREFIX=sms:cortex
OYSTER_PREFIX=sms:oyster
CACHE_PREFIX=sms:cache
CORTEX_TYPE=school-management-api

# ============================================
# JWT SECRETS (GENERATE AND SET IN RENDER DASHBOARD)
# ============================================
# Generate random 64-character hex strings for production
# Command: openssl rand -hex 32
LONG_TOKEN_SECRET=generate-32-char-random-hex-string-in-render-dashboard
SHORT_TOKEN_SECRET=generate-32-char-random-hex-string-in-render-dashboard
NACL_SECRET=dGhpcyBpcyBhIDMyIGNoYXIgbmFjbCBzZWNyZXQga2V5Lg==

# ============================================
# SECURITY & RATE LIMITING
# ============================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

JWT_LONG_EXPIRY=7d
JWT_SHORT_EXPIRY=24h

# ============================================
# APPLICATION FEATURES
# ============================================
ENABLE_DOCS=true
ENABLE_LOGGING=true
DEFAULT_PAGE_LIMIT=20
MAX_PAGE_LIMIT=100
MAX_FILE_SIZE=10
